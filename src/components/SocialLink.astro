---
const { platform, url, iconName, iconType = 'lucide' } = Astro.props;
import Icon from './Icon.astro';
---

<a href={url} class="flex items-center gap-2 text-white no-underline">
  <Icon icon={iconName} iconType={iconType} size={24} />
  <span>{platform}</span>
</a>